package com.mate.cybermate.Service;

import com.mate.cybermate.Config.Role;
import com.mate.cybermate.domain.Member;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
/*

class MemberServiceTest {
    List<Member> memberList=new ArrayList<>();

    Member member= Member.createMember(
            "user1", "123","유저1", Role.MEMBER
    );




    @Test
    void isExistedMember() {

     assertTrue(member!=null);
    }

    @Test
    void saveMember() {
        Member member2= Member.createMember(
                "user12", "123","유저2", Role.MEMBER
        );

        assertEquals("유저2",member2.getNickName());
        assertNotNull(member2);
    }

    @Test
    void findByLoginId() {
        boolean find=false;

        if(member.getLoginId()!=null){
            find=true;
        }

        assertTrue(find);
    }
    @Test
    void isDupleLoginId() {
        boolean isDupleLoginId=false;

        Member member= Member.createMember(
                "user1", "123","유저1", Role.MEMBER
        );

        Member member2= Member.createMember(
                "user1", "123","유저2", Role.MEMBER
        );

        if(member.getLoginId().equals(member2.getLoginId())){
            isDupleLoginId=true;
        }

        assertTrue(isDupleLoginId);
    }

    @Test
    void isDupleNickName() {
        boolean isDupleNickName=false;

        Member member= Member.createMember(
                "user1", "123","유저1", Role.MEMBER
        );

        Member member2= Member.createMember(
                "user1", "123","유저1", Role.MEMBER
        );

        if(member.getNickName().equals(member2.getNickName())){
            isDupleNickName=true;
        }

        assertTrue(isDupleNickName);
    }
}
*/
